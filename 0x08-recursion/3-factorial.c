/**
 * factorial - makes a factorial operation
 * @n: Holds the number to be factored
 * Return: the result
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
